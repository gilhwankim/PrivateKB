/**
 * Document states, chunks, validation policies and processing errors.
 * Independent of HTTP, JDBC and parsing/indexing adapter implementations.
 * Existing Spring component and configuration dependencies are retained.
 */
package io.privatekb.ingestion.internal.domain;
