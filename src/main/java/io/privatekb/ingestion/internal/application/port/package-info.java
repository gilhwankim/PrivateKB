/**
 * Internal contracts for persistence, parsing and background processing.
 * Adapters implement these interfaces; they are not public module APIs.
 */
package io.privatekb.ingestion.internal.application.port;
