/**
 * Registration, status lookup, retry and asynchronous ingestion use cases.
 * Coordinates domain rules and ports without depending on adapter implementations.
 */
package io.privatekb.ingestion.internal.application;
